rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    // VSL Videos - publicly readable, only authenticated users can write
    match /vsl-videos/{videoId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
